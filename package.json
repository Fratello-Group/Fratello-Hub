{
  "name": "fratello-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "seed:firestore": "node scripts/seed-firestore.js",
    "test:firestore-rules": "node firestore-rules.test.js",
    "test:rules:emulator": "firebase emulators:exec --only firestore \"node test/firestore-rules.emulator.test.js\"",
    "test": "npm run test:firestore-rules"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "firebase-admin": "^12.7.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.2",
    "firebase-tools": "^13.0.0"
  }
}
